@page "/getting-started"

<NBTitle Variant="@NBTitleVariant.Title">Installation</NBTitle>
<NBTitle Variant="@NBTitleVariant.Subtitle">Get NeoBrutalism Blazor into your Blazor project in 3 steps.</NBTitle>

<NBTitle Variant="@NBTitleVariant.Section">1. Install the NuGet package</NBTitle>

<CodeBlock Language="bash" Code="dotnet add package NeoBrutalism.Blazor" />

<NBTitle Variant="@NBTitleVariant.Section">2. Add the stylesheet</NBTitle>
<p>In your <code>App.razor</code> (Blazor Web App) or <code>_Host.cshtml</code>, add the design system CSS <strong>before</strong> your own styles:</p>

<CodeBlock Language="html" Code="@CS.GettingStartedLink" />

<NBTitle Variant="@NBTitleVariant.Section">3. Add the global using</NBTitle>
<p>In your <code>_Imports.razor</code>:</p>

<CodeBlock Code="@CS.GettingStartedUsing" />

<p>That's it. All components, enums, and models are now available in every <code>.razor</code> file.</p>

<NBTitle Variant="@NBTitleVariant.Section">Optional: Inter font</NBTitle>
<p>The design system uses Inter. Add this to your <code>&lt;head&gt;</code> for the best results:</p>

<CodeBlock Language="html" Code="@CS.GettingStartedFont" />
