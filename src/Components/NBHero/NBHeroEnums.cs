namespace NeoBrutalism.Blazor;

/// <summary>Layout variant of an <see cref="NBHero"/>.</summary>
public enum NBHeroVariant
{
    /// <summary>Centered text with CTA and optional visual below.</summary>
    Centered,
    /// <summary>Text on the left, visual on the right.</summary>
    Split
}
