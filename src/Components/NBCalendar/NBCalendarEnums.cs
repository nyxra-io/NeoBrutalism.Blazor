namespace NeoBrutalism.Blazor;

/// <summary>Selection mode for the calendar.</summary>
public enum NBCalendarMode
{
    /// <summary>Single date selection.</summary>
    Single,
    /// <summary>Date range selection (start + end).</summary>
    Range
}
