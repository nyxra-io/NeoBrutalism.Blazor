@namespace NeoBrutalism.Blazor
@* Neo Brutalism Card Action — floats to the right in the header *@

<div class="nb-card__action">
    @ChildContent
</div>

@code {
    [Parameter]
    public RenderFragment? ChildContent { get; set; }
}
